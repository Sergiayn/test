name: test

on: [workflow_dispatch]

jobs:
  main:
    name: "deploy"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: t1
        run: ls -la
      - name: install node modules
        run: npm install
      - name: t2
        run: du -sh *


