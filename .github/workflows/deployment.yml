name: test

on: [workflow_dispatch]

jobs:
  main:
    name: "deploy"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: update os
        run: node -v


